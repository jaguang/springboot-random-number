application.data-dir: C:\Users\User\Pictures
spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:api_inventory
    username: root
    password:
  jpa.database-platform : org.hibernate.dialect.H2Dialect
  messages.basename: i18n/messages
  servlet:
    multipart:
      max-file-size: 300KB
      max-request-size: 600KB